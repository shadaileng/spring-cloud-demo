spring:
  profiles:
    active: dev
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888
      name: commons-eureka, commons-admin, commons-zipkin, commons-service, commons-web, sso
      label: dev
      profile: dev
---
feign:
  hystrix:
    enabled: true
spring:
  profiles: prod
  cloud:
    config:
      uri: http://116.85.54.176:8888
      name: commons-eureka, commons-admin, commons-zipkin, commons-config, commons-service, commons-web, sso
      label: dev
      profile: prod
ipreg: localhost
#ipreg: 192.168.0.7
