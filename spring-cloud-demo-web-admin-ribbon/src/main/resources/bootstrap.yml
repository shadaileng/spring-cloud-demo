spring:
  profiles:
    active: dev
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888
      name: commons-eureka, commons-admin, commons-zuul, commons-web, web-admin-ribbon
      label: dev
      profile: dev
---
spring:
  profiles: prod
  cloud:
    config:
      uri: http://116.85.54.176:8888
      name: commons-eureka, commons-admin, commons-zuul, commons-web, web-admin-ribbon
      label: dev
      profile: prod